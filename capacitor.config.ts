import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'PowerUp.Frontend.B2C',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
